# تحدّي — توجيهات المالك الدائمة (لا تُطلب من جديد)

المالك: عبدالرحمن بن سعيد المعشني. اللعبة: `tahaddi/index.html` (ملفّ واحد، عربيّة RTL، هويّة كحليّة وذهبيّة فاخرة).
هذه التوجيهات قالها المالك مرّةً واحدة وطلب حفظها؛ كلّ عملٍ على اللعبة يُقاس بها، ولا يُسأل عنها مرّةً أخرى.

## سقف الجودة (٢٠٢٦-٠٩-٢٤)
1. **كلّ الصفحات بتصاميم واضحة وخاصّة** — لا صفحة عامّة ولا قائمةً خامًا. كلّ شاشة لها تكوينها وعنوانها وأيقوناتها وحركتها.
2. **هويّة اللعبة واضحة** في كلّ شاشة: كحليّ `#0B1530` وذهبيّ `#E8B23A`/`#FFE9A8`، زخرفة هندسيّة عربيّة، خطٌّ عربيّ فاخر، أزرارٌ ذهبيّة مصقولة، زرّ رجوعٍ مصمَّم.
3. **سلاسة عالية**: الهدف ٦٠ إطارًا في الثانية على هاتف أندرويد متوسّط في اللعب وشاشة الفوز والصندوق والرئيسيّة. تُقاس بمحاكاة إبطاء المعالج ×٤ قبل الشحن؛ لا `backdrop-filter` ولا `filter:blur` على طبقاتٍ متحرّكة، ولا حلقات رسمٍ تعمل تحت شاشةٍ مغطّاة.
4. **ثلاثيّة الأبعاد أحيانًا**: عمقٌ حقيقيّ حيث يليق (شاشة الدخول، الساحات، الصناديق، شاشة الفوز): طبقات منظور، ميلٌ بالجهاز، إضاءةٌ وانعكاسات.
5. **عرض الساحات والكؤوس كما في فيديو كلاش رويال** الذي أرسله المالك: طريقٌ عموديّ، كلّ ساحةٍ قسمٌ بلوحته واسمه ورقمه وعدد الكؤوس المطلوب، الجوائز على منصّاتٍ صغيرة بجانب رقم الكأس، الحاليّة مميّزة، استلامٌ في مكانه، زرّ «موافق» ثابت أسفل.
6. **عشر ساحات بعتبات كلاش رويال الأولى**: 0 / 300 / 600 / 1000 / 1300 / 1600 / 2000 / 2300 / 2600 / 3000.
7. **الكتابات أفضل**: خطّ عرضٍ عربيّ قويّ للعناوين والأرقام، خطّ نصٍّ واضح، مقياس أحجامٍ ثابت، تشكيلٌ صحيح، لا كلمة لاتينيّة ظاهرة، لا إيموجي.
8. **الشخصيّات والتعابير برسمٍ مصقول** (أسلوب البطاقات المرسومة)، متّسقة فيما بينها.
9. **OpenArt يُستعمل في كلّ صورةٍ فنّيّة** (ساحات، شاشات مفتاحيّة، شخصيّات، أيقونات، عُدّة الأزرار) بحساب المالك. الصور لا تصل إلى بيئة العمل إلا إذا أرسلها المالك في الدردشة؛ أدوات التضمين جاهزة: `tools/build-keyart.py` و`tools/build-emotes-v3.py`.
10. **شاشة الفوز**: لا تتحرّك بالإصبع، لا تُقصّ، صوتٌ واحدٌ مصمَّم للفوز وآخر للخسارة، بلا أصواتٍ رخيصة، بلا طبقاتٍ تتراكب فوقها، تسلسلٌ هرميّ: تيجان، لوحة، الخصم، النتيجة، الكؤوس بعدٍّ متحرّك، الجوائز، الأزرار مثبّتة.
11. **زرّ اختيار الجيس وقدراته ظاهر** من الرئيسيّة وردهة الكيرم وقبل المباراة.
12. **خذ كامل وقتك ولا تستعجل**؛ قِس ولا تدّعِ؛ لقطات الشاشة دليل؛ لا تراجع في أيّ ميزة سبق شحنها.

## قواعد العمل الثابتة
- التعديل بمقاطع بايثون ذات مراسٍ مؤكَّدة العدد، ثمّ: `node tools/splash-total.cjs` → تحليل acorn → `npm run game:check` → i18n وemoji → الاختبارات → `node tools/qa-crawl.cjs` → لقطات → التزام ودفع → `node tools/build-www.cjs` → نشر المعاينة.
- الردّ على المالك بالعربيّة، ملخّصٌ منظّم، مع اللقطات.
- لا مفاتيح سرّيّة في المستودع (Brevo، service_role)؛ لا معرّفات نماذج في الالتزامات.
